<% if post.user.avatar_file_name.nil? %>
  <%= link_to gravatar_for(post.user, options = { size: 40 }), user_path(post.user), class: "float-left" %>
<% else %>
  <%= link_to( image_tag(post.user.avatar.url(:tiny)), user_path(post.user), class: "float-left gravatar") %>
<% end %>
